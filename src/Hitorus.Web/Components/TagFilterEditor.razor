<div class="gap-4 align-center" style="display: grid; grid-template-columns: auto auto 1fr auto auto;">
    <MudTooltip Text=@Localizer["Create"]>
        <MudFab Color="Color.Primary"
                StartIcon="@Icons.Material.Filled.Add"
                OnClick="@OnCreateButtonClicked" />
    </MudTooltip>
    <MudTooltip Text=@Localizer["Rename"]>
        <MudFab Color="Color.Primary"
                StartIcon="@Icons.Material.Filled.Edit"
                OnClick="@OnRenameButtonClicked"
                Disabled="@(CurrentTagFilter == null)" />
    </MudTooltip>
    <MudSelect T="TagFilterDTO"
               Variant="Variant.Outlined"
               Placeholder=@Localizer["SelectPlaceholder"]
               @bind-Value="@CurrentTagFilter"
               ToStringFunc="@(tf => tf.Name)"
               Class="tag-filter-selector"
               FullWidth>
        @foreach (TagFilterDTO tagFilter in TagFilters) {
            <MudSelectItem Value="@tagFilter" />
        }
    </MudSelect>
    <MudTooltip Text=@Localizer["Save"]>
        <MudFab Color="Color.Primary"
                StartIcon="@Icons.Material.Filled.Save"
                OnClick="@OnSaveButtonClicked"
                Disabled="@(CurrentTagFilter == null)" />
    </MudTooltip>
    <MudTooltip Text=@Localizer["Delete"]>
        <MudFab Color="Color.Primary"
                StartIcon="@Icons.Material.Filled.Delete"
                OnClick="@OnDeleteButtonClicked"
                Disabled="@(CurrentTagFilter == null)" />
    </MudTooltip>
</div>